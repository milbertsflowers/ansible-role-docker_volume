galaxy_info:
  role_name: docker_volume
  author: milbertsflowers
  description: Creates a docker volume with automatic backup to a compressed `tar` archive using cron.
  license: MIT
  min_ansible_version: 2.10
  platforms:
    - name: EL
      versions:
        - all
    - name: Debian
      versions:
        - all
  galaxy_tags:
    - docker
    - volume
    - backup
dependencies: []
